defmodule ElixirPhoenixRealtimeAnalyticsDashboardTest do
  use ExUnit.Case

  test "basic functionality" do
    assert 2 + 2 == 4
  end

  test "project files exist" do
    assert File.exists?("README.md")
    assert File.exists?("LICENSE")
  end
end
